# Binary-Search-Tree
To run the code, download/clone all the files, and on the terminal/Ubuntu follow these steps:
1. run makefile by simply writing the command: make
2. run the executable by writing this command: ./lab10 pokemon.txt
